# Optional public display name; the application works without this file.
# Nombre público opcional; la aplicación funciona sin este archivo.
# Every VITE_ value is public client configuration, never a secret.
# Todo valor VITE_ es configuración pública del cliente, nunca un secreto.
VITE_APP_NAME=Project Base
